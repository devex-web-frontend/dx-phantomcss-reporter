@import '../../config.styl';

.header {
	padding: 20px;
	font-size: 20px
	display: flex;
	align-items: center;
	border-bottom: 5px solid;

	&--title {
		margin-right: auto;
	}
	&--link {
		font-size: 16px;
		&:hover {
			text-decoration: underline;
		}
	}
}